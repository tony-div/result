{
  "name": "@moftaked/result",
  "version": "0.0.1",
  "files": [
    "./dist"
  ],
  "description": "rust inspired errors in js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf ./dist",
    "build": "npm run clean && tsup src/index.ts --format cjs,esm --dts"
  },
  "keywords": [
    "rust",
    "error",
    "result",
    "errors",
    "error-handling"
  ],
  "author": "Tony George",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
